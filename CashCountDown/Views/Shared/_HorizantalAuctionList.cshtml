@model IEnumerable<CashCountDown.Models.Auction>

<ul class="horizontal-bid-list">
    @{if (Model != null)
      {
          foreach (var auction in Model)
          { Html.RenderPartial("_AuctionCard", auction); }
      }
    }

</ul>




